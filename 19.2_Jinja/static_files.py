# Jinja can be used to serve static files, such as JS and CSS;
# To do this, simply link to them in individual templates.

# In practice, we would link the majority of code in a base template:
    # Extend this template:
      # then link new files only for specific style/behavior.

